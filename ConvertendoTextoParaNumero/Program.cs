using System;
using System.Globalization;

namespace curso
{
    class Progam
    {
        static void Main(string[] args)
        {
            string NumeroTexto = "30";

            int Numero1 =  int.Parse(NumeroTexto);
            int Numero2 = 30;

            Console.WriteLine("O resultado da soma é: " + (Numero1 + Numero2));
            /*Precisa colocar entre ( ) para existir a soma */


        }
    }
}
